class Sequence{
private val = 0;
private step = 10;

    int next_val(){
        if (30 == val) val += 10;
        val += step
    }
}

Sequence seq = new Sequence()

List sl = [ seq.next_val(), seq.next_val(), seq.next_val(), seq.next_val(), seq.next_val(), seq.next_val() ]

(10..60).step(10) - sl